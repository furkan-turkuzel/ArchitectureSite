@model EkinciProject.Web.Models.ServiceViewModel

<div class="col-md-10 p0">
    <div class="collapse navbar-collapse" id="min_navbar">
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown submenu">
                <a href="@Url.Action("Index","Index")" class="dropdown-toggle" data-toggle="dropdown">Anasayfa</a>
            </li>
            <li class="dropdown submenu">
                <a href="@Url.Action("Index","About")">Hakkımızda</a>
            </li>
            <li class="dropdown submenu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Hizmetlerimiz</a>
                <ul class="dropdown-menu other_dropdwn">
                    @foreach (var item in Model.ServiceList)
                    {
                        <li><a href="@Url.Action("Service","Service",new { Id = item.Id})">@item.Title</a></li>
                    }
                </ul>
            </li>
            <li class="dropdown submenu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">İmar Barışı</a>
                <ul class="dropdown-menu">
                    <li><a href="@Url.Action("Index","Reconstruction")">İmar Barışı Detayları</a></li>
                    <li><a href="@Url.Action("Index","MostAsked")">Sık Sorulan Sorular</a></li>
                </ul>
            </li>
            <li><a href="@Url.Action("Index","News")">Haberler</a>
            </li>
            <li><a href="@Url.Action("Index","Reference")">Referanslarımız</a></li>
            <li><a href="@Url.Action("Index","Contact")">İletişim</a></li>
        </ul>
    </div><!-- /.navbar-collapse -->
</div>
